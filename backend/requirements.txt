# Vercel installs from this file. Anything imported at runtime must be named
# here even if it arrives locally as somebody else's transitive dependency —
# email-validator did exactly that, so EmailStr worked on this machine and the
# serverless function crashed on import.
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic[email]>=2.0.0
email-validator>=2.0.0
pydantic-settings>=2.0.0
# Arrives via pydantic-settings today; named here so it cannot vanish
# when that changes.
python-dotenv>=1.0.0
# Pinned, not floating.
#
# `supabase>=1.0.3` meant every deploy installed whatever was newest, so
# the app could break without a line of code changing -- and it silently
# moved from 1.x to 2.28. It also drags in storage3, which hard-depends on
# pyiceberg: 867 ms of cold start, measured, for Supabase Storage this app
# never touches. Worth revisiting if that dependency is ever dropped.
supabase==2.28.3
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# openpyxl reads the .xlsx uploads; the standard library reads .csv.
#
# pandas used to do both. It was three calls -- read_csv, read_excel and
# isna -- and it cost 60 MB, dragged in 31 MB of numpy, and from version
# 3.0 another 84 MB of pyarrow as a hard dependency. Because the pin was
# `pandas>=2.0.0`, that 3.0 release arrived on its own and took the
# serverless bundle to 229 MB against a 225 MB ceiling, so every
# deployment failed. See app/services/sheet_parser.read_sheet.
openpyxl>=3.1.0
python-multipart>=0.0.6
httpx>=0.24.0
